{% extends 'FrontOfficeBundle::layout.html.twig' %}

    {% block content %}

        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                    <hr>
                    <h2 class="intro-text text-center">Déposez votre annonce :
                    </h2>
                    <hr>
                    <p>{{ form(formAnnonce) }}</p>
                </div>
            </div>
        </div>

    {% endblock %}