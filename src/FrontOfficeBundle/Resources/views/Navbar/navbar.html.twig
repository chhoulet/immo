<nav class="navbar navbar-default" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <!-- navbar-brand is hidden on larger screens, but visible when the menu is collapsed -->
            <a class="navbar-brand" href="index.html">Découvrez nos programmes neufs :</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <a href="{{ path('front_office_homepage') }}">Home</a>
                </li>
                {% if( is_granted('ROLE_USER')) %}
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}">Déconnection</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('fos_user_security_login') }}">Connection</a>
                    </li>
                {% endif %}
                {% if(is_granted('ROLE_ADMIN')) %}
                    <li>
                        <a href="{{ path('back_office_homepage') }}">Back-Office</a>
                    </li>
                {% endif %}
                    <li>
                        <a href="{{ path('front_office_layout_about') }}">About</a>
                    </li>
                    <li>
                        {#{{ nbAnnonces }} annonces dans votre panier#}
                    </li>
                    <li>
                        <a href="{{ path('front_office_blog_showArticles') }}">Blog</a>
                    </li>
                    <li>
                        <a href="{{ path('front_office_layout_contact') }}">Contact</a>
                    </li>
                    <li>
                        <a href="{{ path('front_office_basket_list') }}">Voir mon panier</a>
                    </li>
                    <li>
                        {#{{  render(controller('FrontOfficeBundle:Basket:countAnnonces')) }}#}
                    </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>

